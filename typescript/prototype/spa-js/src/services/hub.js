export const ConnectionState = {
  CONNECTED: 0,
  CONNECTING: 1,
  DISCONNECTED: 2,
  RECONNECTING: 3
}

const hub = {
  buildConnection(userName) {
  },
  start() {
  },
  close(connection) {
  }
}

export default hub
